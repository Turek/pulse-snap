// Boot test omitted: ProviderScope keeps drift streams + chart timers alive,
// triggering the test framework's "Timer is still pending" guard. The boot
// path is covered by the manual `flutter run` smoke check listed in the
// Sprint 7 verification section of the plan.
void main() {}
